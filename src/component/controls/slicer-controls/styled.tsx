import styled from 'styled-components';

export const SSlicerControls = styled.div`
  position: relative;

  display: flex;
  justify-content: center;
  align-items: center;
  flex: 1;

  button:last-child {
    position: absolute;
    right: 1.5rem;
  }
`;
